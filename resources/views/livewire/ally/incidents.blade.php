<div class="space-y-6">
    <div><h1 class="font-display text-2xl font-bold text-slate-900">Incidencias</h1><p class="mt-1 text-sm text-slate-500">Registra y consulta incidencias de los envíos de tu agencia.</p></div>
    @if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700">{{ session('success') }}</div>@endif
    <div class="rounded-2xl border bg-white p-5 shadow-sm"><form wire:submit.prevent="create" class="space-y-4"><div><label class="text-sm font-medium">Número de guía</label><input wire:model="tracking" class="mt-1 w-full rounded-xl border px-4 py-3" placeholder="VEN-...">@error('tracking')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-medium">Tipo</label><input wire:model="type" class="mt-1 w-full rounded-xl border px-4 py-3"></div><div><label class="text-sm font-medium">Descripción</label><textarea wire:model="description" rows="4" class="mt-1 w-full rounded-xl border px-4 py-3"></textarea>@error('description')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</div><button class="rounded-xl bg-blue-900 px-5 py-3 font-semibold text-white">Registrar incidencia</button></form></div>
    <div class="space-y-3">@forelse($incidents as $incident)<div class="rounded-2xl border bg-white p-5"><div class="flex justify-between gap-3"><div><div class="font-semibold">{{ $incident->type }}</div><div class="font-tracking text-xs text-slate-500">{{ $incident->package?->tracking_number }}</div></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs">{{ $incident->status }}</span></div><p class="mt-2 text-sm text-slate-600">{{ $incident->description }}</p></div>@empty<div class="rounded-2xl border bg-white p-8 text-center text-slate-500">Sin incidencias.</div>@endforelse</div>{{ $incidents->links() }}
</div>
